import React from 'react';
import Card from '../Card';
// import renderer from 'react-test-renderer';

describe('Card testing', () => {
  describe('Card initialization', () => {
    it('Children props are not empty', () => {
      });
    });
  });
});

